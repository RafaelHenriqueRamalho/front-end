body{
    font-family: Arial, Helvetica, sans-serif;
    display: flex;
    flex-direction: column;
    margin: 0;
    height: 100vh;
}


/* header */

header{
    background-image: linear-gradient(#006699, #006688);
}

footer {
    background-color: #0ad146;
    margin-top: auto;
}
/* main */
main {
    background-image: linear-gradient(rgb(153, 149, 149));
}
